apiVersion: v1
kind: Service
metadata:
  name: myapp-SVC
  labels:
    app: myapp
spec:
  type: NodePort
  ports:
  - ports: 8080
     nodeport: 80001
     protocol: TCP
  selector:
    app: myapp
    